{
  "id": "amm",
  "name": "AMM Memory",
  "description": "Durable memory substrate for OpenClaw agents via amm. Injects ambient recall before each LLM call and captures conversation events.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "ammBin": {
        "type": "string",
        "description": "Path to the local amm binary. Defaults to 'amm' on PATH.",
        "default": "amm"
      },
      "dbPath": {
        "type": "string",
        "description": "Path to the amm SQLite database (binary mode only). Defaults to ~/.amm/amm.db."
      },
      "apiUrl": {
        "type": "string",
        "description": "Base URL for the AMM HTTP API. When set, the plugin uses HTTP transport instead of the local binary."
      },
      "apiKey": {
        "type": "string",
        "description": "Bearer token for AMM HTTP API authentication."
      },
      "projectId": {
        "type": "string",
        "description": "Stable project identifier for scoped recall. Also settable via AMM_PROJECT_ID env."
      },
      "recallLimit": {
        "type": "integer",
        "description": "Maximum number of recall items to inject per turn.",
        "default": 5,
        "minimum": 1,
        "maximum": 25
      },
      "syncCuratedMemory": {
        "type": "boolean",
        "description": "Enable curated memory mirroring from MEMORY.md/USER.md to AMM durable memories.",
        "default": false
      },
      "curatedProjectId": {
        "type": "string",
        "description": "Override project ID for curated memory writes. Falls back to projectId."
      },
      "memoryScope": {
        "type": "string",
        "description": "AMM scope for MEMORY.md entries.",
        "default": "project",
        "enum": ["global", "project"]
      },
      "userScope": {
        "type": "string",
        "description": "AMM scope for USER.md entries.",
        "default": "global",
        "enum": ["global", "project"]
      },
      "memoryType": {
        "type": "string",
        "description": "AMM memory type for MEMORY.md entries.",
        "default": "fact"
      },
      "userType": {
        "type": "string",
        "description": "AMM memory type for USER.md entries.",
        "default": "preference"
      }
    }
  }
}
